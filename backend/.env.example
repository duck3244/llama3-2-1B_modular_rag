# 서버 시작 시 자동으로 활성화할 PDF 경로 (선택)
# 비워두면 /api/upload로 업로드 후 활성화됩니다.
RAG_DEFAULT_PDF=llama_modular_rag/PLAYGROUND_JUNGGU.pdf

# 로그 레벨: DEBUG / INFO / WARNING / ERROR
LOG_LEVEL=INFO

# CPU 스레드 수 (미지정시 os.cpu_count() 사용)
# RAG_NUM_THREADS=8

# 업로드 한도 (MB)
MAX_UPLOAD_MB=50
